// 宿主进程 → 沙箱进程。**只有这一个方法，而且只有 String。**
//
// ## 为什么参数是 JSON 文本而不是若干字段
//
// 因为「能跨进程的东西」和「能序列化成 JSON 的东西」最好是同一份定义
// （见 `ScriptRequest` 的 KDoc）。中间多一层手写的字段搬运，就会多一处
// 「本地跑得通、过到沙箱少一个字段」的机会 —— 而那正是最难查的一类问题。
// 两边解的是**同一个 `@Serializable` 类**，字段名不可能对不上。
//
// ## 为什么没有超时参数、没有取消方法
//
// 因为这两件事都不该由被调方实现。解释器内部没有任何可用的中断入口
// （实测 `while(true)` 会把引擎线程永久占死、连 `close()` 都不返回），
// 所以调用方唯一的办法是**断开绑定** —— 沙箱在 `onUnbind` 里自杀，
// 那个转不停的线程跟着进程一起消失。超时因此完全在客户端实现，
// 这个接口不需要知道有超时这回事。
//
// ## 返回值
//
// `ScriptOutcome` 的 JSON。**它不表达「我超时了」或「我崩了」** ——
// 那两种情况这个方法根本不会返回，客户端靠「调用抛异常 + 读死因文件」区分。
package com.aichat.sandbox;

interface IScriptSandbox {
    String execute(String requestJson);
}
